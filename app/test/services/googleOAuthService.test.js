var assert = require('chai').assert;
var GoogleOAuthService = require('../../../build/services/oauth/googleOAuthService').GoogleOAuthService;

describe('GoogleOAuthService', function () {
    describe('...', function (){
        it('should ...', function () {
            
            //assert.equal(new GoogleOAuthService(), '');
        });
    });
});