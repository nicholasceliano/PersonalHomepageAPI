var assert = require('chai').assert;
var OpenWeatherMapService = require('../../../build/services/openWeatherMapService').OpenWeatherMapService;

describe('OpenWeatherMapService', function () {
    describe('...', function (){
        it('should ...', function () {
            
            //assert.equal(new OpenWeatherMapService(), '');
        });
    });
});